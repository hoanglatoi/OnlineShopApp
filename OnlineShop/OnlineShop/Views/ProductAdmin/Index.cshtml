@model IEnumerable<OnlineShop.Model.Models.ProductCategory>

@{
    ViewData["Title"] = "Product For Admin";
    Layout = "_AdminLayout";
}

<div class="row">
    <div class="col-sm-6">
        <h1>Danh Sách Sản Phẩm</h1>
    </div>
    <div class="col-sm-6">
        <p style="padding-top: 5px">
            <a asp-action="Create">Thêm Danh Mục Mới</a>
        </p>
    </div>
</div>

<form asp-controller="ProductAdmin" asp-action="Index" method="get" style="padding-top: 20px">
    <p>
        Tên Danh Mục: <input type="text" name="SearchString" />
        <input class="btn btn-primary" type="submit" value="Tìm Danh Mục" />
        <a asp-action="Index" class="btn btn-primary">Xem Tất Cả Danh Mục</a>
    </p>
</form>

<table class="table">
    <thead>
        <tr>
            <th>
                ID
            </th>
            <th>
                Tên Danh Mục
            </th>
            <th>
                Tiêu Đề
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ID)
                </td>
                <td>
                    <a asp-action="IndexProducts" asp-route-id="@item.Name">@Html.DisplayFor(modelItem => item.Name)</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.MetaTitle)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.ID">Sửa Thông Tin</a> |
                    <a asp-action="ViewDetails" asp-route-id="@item.ID">Chi Tiết</a> |
                    <a asp-action="Delete" asp-route-id="@item.ID">Xóa Danh Mục</a>
                </td>
            </tr>
        }
    </tbody>
</table>